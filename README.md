# Car Rent App

## Getting Started
- Ensure Flutter (3.9.0 or newer) and Dart SDK are installed locally.
- Install dependencies: `flutter pub get`.
- Run the app on a simulator or device: `flutter run`.

